<div id="productCategories">
		<h3>Product Categories<h3>
		<ul>
	   	<% @categories.each do |c| %>
	   		<li class="categoryItem"><%= link_to(c.name,category_path( c )) %></li>
		<% end %>
		</ul>
</div>